
// Assuming num1 and num2 are defined somewhere in your code
const num1 = 31; // Example value
const num2 = 2;  // Example value
const multiply = num1 * num2; // This should equal 62

// Generate a random integer greater than 0
const random = Math.floor(Math.random() * 100) + 1; // Generates a random integer between 1 and 100

// Assuming num3 and num4 are defined somewhere in your code
const num3 = 10; // Example value
const num4 = 6;  // Example value
const mod = num3 % num4; // This should equal 4

// Find the maximum number in a set
const numbers = [5, 10, 15, 20]; // Example set of numbers
const max = Math.max(...numbers); // This should return 20